package com.ifmg.apipolo.repository;

import com.ifmg.apipolo.entity.Edicts;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EdictsRepository extends JpaRepository<Edicts, Long> {
}
